version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    volumes:
      - baileys_sessoes:/app/sessoes
    restart: unless-stopped

volumes:
  baileys_sessoes:
